You are the DevOps and system architect for the AuditWise SaaS platform.

Objective:
Implement a full production deployment pipeline that connects Replit development with the Hostinger VPS deployment environment.

System components:
Frontend: React / Next.js
Backend: Node.js Express
Process manager: PM2
Server: Ubuntu VPS
Repository: GitHub

Repository URL:
[git@github.com](mailto:git@github.com):aqeelalamfca-sys/Test-Audit.git

Deployment target:
Hostinger VPS (Ubuntu 24.04)

Tasks to implement:

1. Create a deployment service module inside the backend that:

   * pulls code from GitHub
   * installs dependencies
   * builds the application
   * restarts the PM2 service
   * logs deployment activity

2. Create a Super Admin monitoring dashboard page titled:
   "Enterprise-Grade Checklist"

3. Dashboard must fetch live server data from the VPS monitoring API.

Display cards for:
• repository status
• deployment pipeline status
• server health
• application runtime
• security checks
• continuous deployment status

Use animated UI with:
• TailwindCSS
• gradient cards
• live progress bars
• animated GIF indicators
• green/orange/red status badges
• auto refresh every 20 seconds.

4. Backend API endpoint:

/api/system-health

This endpoint retrieves server metrics including:

git status
git log
pm2 status
server uptime
CPU usage
memory usage
disk usage
deployment logs

Return JSON data.

5. Implement WebSocket updates for live metrics.

6. Only the Super Admin role can access this dashboard.

7. Implement deployment logs storage in:

/logs/deployments.log

8. Add navigation path:

Super Admin → System Monitoring → Enterprise-Grade Checklist.

9. Ensure all server commands execute through secure SSH connections using environment variables.

10. Provide error alerts, deployment progress animation, and live status indicators.

The dashboard must act as a single operational control center for monitoring the SaaS deployment infrastructure.
