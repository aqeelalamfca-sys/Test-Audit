Build a professional Excel workbook generator in Replit named “AuditWise_Data_Validation_Workbook.xlsx” using Python and openpyxl. The workbook must validate 5 source datasets separately and consolidately: Trial Balance, General Ledger, Bank, Accounts Receivable, and Accounts Payable. Use GL Code as the primary matching key, treated as text, normalized by trimming spaces and preserving leading zeros.

Create exactly 5 formatted sheets:

1. Control_Summary
Create a dashboard showing client info, period, prepared by, reviewed by, import date, materiality, row counts, unique GL code counts, matched codes, missing codes, duplicate codes, amount mismatches, blank mandatory fields, invalid dates, unmapped balances, validation status, consolidated reconciliation, traffic light indicators, hyperlinks to detail sheets, and final auditor remarks.

2. TB_Validation
Validate TB for unique GL codes, blank fields, duplicate codes, zero balances, sign issues, mapping status, formula logic, and cross-cast checks. Add validation status and review remarks.

3. GL_vs_TB_Validation
Summarize GL by GL Code and compare with TB movement. Show matched codes, missing codes, account name mismatches, movement differences, invalid dates, duplicate transactions, blank narration, review remarks, and validation status.

4. Subledger_Validation
Create 3 sections in one sheet: Bank, AR, and AP. Compare each with TB and GL. Show GL Code, source total, TB balance, GL balance/activity, difference vs TB, difference vs GL, missing code flags, duplicate flags, sign flags, review remarks, and validation status. Provide subtotals and exception counts for each section.

5. Exceptions_Report
Create a centralized exception log with Exception ID, source area, source file, GL Code, account name, exception category, description, amount difference, severity, action required, assigned to, status, reviewer comment, management explanation, final resolution, and date cleared.

Workbook requirements:
- professional audit-friendly formatting
- freeze panes and filters on all sheets
- Excel tables where practical
- color-coded inputs, formulas, linked fields, warnings, and exceptions
- dropdowns for status fields
- conditional formatting for duplicates, blanks, unmatched codes, non-zero differences, and failed validations
- formulas only, no hardcoded summary outputs
- helper logic may be placed in hidden columns/sheets
- formula cells protected if feasible, input cells editable
- top navigation links back to Control_Summary
- no broken formulas or Excel errors

Validation logic:
- compare TB vs GL
- compare Bank vs TB
- compare AR vs TB
- compare AP vs TB
- compare subledgers vs GL where relevant
- highlight all unmatched GL codes and amount differences
- show separate validation and consolidated validation

Output:
Generate one final workbook named exactly “AuditWise_Data_Validation_Workbook.xlsx” with sample formulas, sample formatting, and sample test rows so the file is immediately usable and understandable.