You are working on AuditWise (React frontend + .NET API + SQL). Implement two features across the entire audit lifecycle (Pre-Planning, Planning, Execution, FS Heads, Evidence, Finalization, Deliverables, Standards/QR). Keep changes consistent across every module page/tab/sub-tab.

========================================================
A) UNIFIED SIGN-OFF (SINGLE “COMPLETE” BUTTON + ONE BAR)
========================================================

1) UI CHANGES (ALL MODULE PAGES)
- Remove existing action buttons: “Prepare”, “Review”, “Approve”.
- Remove separate “Manager Sign-Off” and “Partner Sign-Off” blocks.
- Add a single reusable component <SignOffBar /> placed at the top-right (or consistent location) on every module page/tab/sub-tab.
- <SignOffBar /> must display ONE bar with:
  - Prepared By: {PreparedUserName} (Associate) + PreparedOn (server timestamp)
  - Reviewed By: {ReviewedUserName} (Manager) + ReviewedOn (server timestamp)
  - Approved By: {ApprovedUserName} (Partner) + ApprovedOn (server timestamp)
  - Status badge: Draft / Prepared / Reviewed / Approved
  - A single action button: “Complete” (only visible when the logged-in role is allowed for the current status)
- Color states:
  - Draft = Grey, Prepared = Blue, Reviewed = Orange, Approved = Green (Locked)
- Show tick icons when each sign-off exists.
- Display timezone as PKT for UI (but store UTC on server).

2) RBAC + SEQUENTIAL ENFORCEMENT
- Only Associate can Complete when status=Draft → sets Prepared fields.
- Only Manager can Complete when status=Prepared → sets Reviewed fields.
- Only Partner can Complete when status=Reviewed → sets Approved fields + locks module.
- Disallow skipping; show clear error messages.
- After Approved: make the module read-only (disable inputs, add/edit/delete actions, hide Complete button, show “Approved – Read Only” badge). Admin override optional.

3) DATABASE CHANGES (APPLY TO EVERY MODULE TABLE OR A COMMON SIGNOFF TABLE)
Add these columns (or create a shared module_signoff table keyed by module_name+module_id):
- status (varchar(20)) default 'Draft'  -- Draft|Prepared|Reviewed|Approved
- prepared_by_user_id (int, FK Users) nullable
- prepared_at (datetime, UTC) nullable
- reviewed_by_user_id (int, FK Users) nullable
- reviewed_at (datetime, UTC) nullable
- approved_by_user_id (int, FK Users) nullable
- approved_at (datetime, UTC) nullable
- lock_flag (bit) default 0

4) SERVER SIDE ENDPOINTS
Implement:
- GET /api/signoff/{moduleName}/{moduleId}  -> returns status + user names + timestamps + lock flag
- POST /api/signoff/{moduleName}/{moduleId}/complete -> performs the state transition (Draft→Prepared, Prepared→Reviewed, Reviewed→Approved) based on logged-in role, using SERVER UTC time only (no client time).
- POST /api/signoff/{moduleName}/{moduleId}/unlock (Admin only, optional)

5) AUDIT TRAIL (MANDATORY)
Create table audit_signoff_log:
- log_id PK, module_name, module_id, action, performed_by_user_id, performed_at(UTC), previous_status, new_status, ip_address, device_info
Insert a row on every transition and unlock.

6) FRONTEND BEHAVIOR
- Fetch signoff info on page load and after Complete.
- Complete button rendering logic:
  - status Draft + role Associate -> show Complete
  - status Prepared + role Manager -> show Complete
  - status Reviewed + role Partner -> show Complete
  - else hide Complete
- If Approved -> force read-only view everywhere on that module.

========================================================
B) FINALIZATION CONTROL BOARD (USER-WISE + TIMESTAMP + AI RISK)
========================================================

1) UI (FINALIZATION STAGE ONLY)
Add a “Finalization Control Board” section with tiles like:
- Pending Items from Execution (Read-only tile + drilldown)
- Unadjusted Differences Summary (ISA 450) (Read-only + drilldown)
- Key Audit Matters Summary (ISA 701) (Read-only + drilldown)
Display header:
- Engagement Name
- Logged-in User Name + Role
- Generated On: {server timestamp} (PKT display)

2) USER-WISE FILTERING + ROLE SCOPE
- Associate: sees only items assigned to self (execution pending, own open issues), plus engagement-level ISA 450/701 totals in read-only.
- Manager: sees team-wide view (all users in engagement).
- Partner: sees engagement-wide executive summary.
- Admin: per permissions.

3) DATA MODEL (choose Snapshot or Dynamic View; implement Snapshot preferred)
Create table user_finalization_board (snapshot per user per engagement per generate):
- id PK, engagement_id, user_id, pending_execution_items, unadjusted_misstatements, kam_items, risk_score, risk_rating, risk_drivers_json, generated_at(UTC), generated_by_user_id
Also log “Board Generated/View” to audit_signoff_log.

4) BOARD GENERATION + API
Implement:
- POST /api/finalization/{engagementId}/generate-board
  - uses logged-in user role to compute scope
  - queries sources:
    a) pending execution items assigned/open
    b) ISA 450: unadjusted misstatements
    c) ISA 701: KAM reporting_required
    d) open issues by severity + age
    e) evidence completeness flags
  - saves snapshot to user_finalization_board with server UTC timestamp
- GET /api/finalization/{engagementId}/board
  - returns latest snapshot for that user and scope (associate/team/engagement)
- Add drilldown routes with filters (engagement_id + assigned_to + status/severity)

5) AI-ASSISTED RISK SCORING (AUDIT-FRIENDLY, EXPLAINABLE)
Compute a deterministic base score (0–100) + AI narrative drivers:
Base scoring example:
- open execution items: min(30, open_items*2)
- high severity issues: min(30, high*10)
- medium severity: min(15, med*3)
- missing evidence flags: min(15, missing_evidence*5)
- unadjusted near materiality: +10
- KAM required but draft incomplete: +10
Risk Rating:
- 0–29 Low, 30–59 Medium, 60–100 High
AI output must produce:
- Top 3 drivers (plain English)
- Recommended next actions (links to drilldowns)
Store drivers in risk_drivers_json.

6) OPTIONAL STANDARDS GATE (FINALIZATION APPROVAL)
If Partner attempts to “Complete” Finalization module:
- Block if risk_rating=High AND unresolved High severity issues > 0 (configurable)
- Block if pending_execution_items > 0 (configurable)
- Block if ISA 450 unadjusted items not documented with rationale (configurable)
Return clear blocker messages and links.

========================================================
DELIVERABLES / ACCEPTANCE
========================================================
- Apply sign-off consistently across ALL modules.
- Ensure timestamps are server UTC, displayed in PKT.
- Ensure UI shows one Sign-Off Bar only (no duplicate blocks).
- Ensure Complete is role-gated and sequential.
- Ensure Approved locks module (read-only).
- Ensure audit trail is written for signoff + board actions.
- Ensure Finalization Board is user-specific and role-scoped with drilldowns.
- Ensure AI risk score is explainable (drivers + actions) and stored.

Now implement the above end-to-end with migrations, API controllers/services, React components, and route wiring. Include minimal seed data for roles/users: Saqib=Associate, Zeeshan=Manager, Aqeel Alam=Partner for testing. Do not break existing pages; refactor to reuse <SignOffBar /> and <FinalizationControlBoard /> components.