You are working in AuditWise (Next.js + .NET API + Postgres) as Platform Super Admin module. Update the current “Plan Management” screen (currently showing PKR 49/199/499 plans) into a professional, multi-office / multi-partner audit-firm SaaS pricing & enforcement system.

GOAL
Replace the current 3 low-priced plans with 4 firm-grade plans and implement full subscription lifecycle, invoicing, limits, overages, and Super Admin controls. Keep the UI layout same style (3–4 plan cards, “Create Plan” button, clean modern tiles), but update all plan content, database, API, and enforcement.

A) NEW PLANS (PKR / Monthly)
Create/seed these plans in DB and show them in Plan Management cards:

1) STARTER (Small Firm)
- Price: PKR 4,900 / mo
- Included Users: 5
- Included Offices: 1
- Included Engagements: 15 per year (or define as per billing year)
- AI: Platform AI included (shared)
- Custom AI key: Not allowed
- Storage: 5 GB

2) GROWTH (Mid Firm)
- Price: PKR 14,900 / mo
- Included Users: 20
- Included Offices: 3
- Included Engagements: 75 per year
- AI: Platform AI included
- Custom AI key: Allowed
- Storage: 25 GB

3) PROFESSIONAL (Large Firm)
- Price: PKR 34,900 / mo
- Included Users: 60
- Included Offices: 7
- Included Engagements: 250 per year
- AI: Platform AI included
- Custom AI key: Allowed
- Storage: 100 GB

4) ENTERPRISE (Network / National)
- Price: PKR 79,900 / mo
- Included Users: Unlimited
- Included Offices: Unlimited
- Included Engagements: Unlimited
- AI: Platform AI included
- Custom AI key: Allowed
- Storage: 500 GB
- Support: Priority + Dedicated Manager (display label only)

B) OVERAGE / ADD-ON PRICING (Monthly)
Implement overage rules (store in plans table and use for invoicing):
- Starter: extra user PKR 800, extra office PKR 2,500, engagement pack 10 for PKR 2,000
- Growth: extra user PKR 700, extra office PKR 2,000, engagement pack 25 for PKR 4,000
- Professional: extra user PKR 600, extra office PKR 1,500, engagement pack 50 for PKR 6,000
- Enterprise: default overages disabled (or keep 0) because unlimited

C) DATABASE (Postgres) — IMPLEMENT THESE TABLES
Implement/verify tables and enums (idempotent migrations):
- plans (limits + overage pricing + AI settings + storage)
- firms
- firm_offices
- subscriptions (firm_id unique, status, trial dates, billing period, grace)
- subscription_addons (optional)
- subscription_entitlements (effective limits; derived from plan + addons)
- subscription_usage (periodic snapshot)
- invoices + invoice_lines
- payments
- billing_audit_log
Use enum subscription_status: trialing, active, past_due, grace, suspended, canceled, expired.
Use invoice_status: draft, issued, paid, void, uncollectible.
Use payment_status: pending, succeeded, failed, refunded.

D) SUBSCRIPTION LIFECYCLE + ENFORCEMENT
Implement these rules:
1) Trial: default 14 days on new firm subscription
- status=trialing, trial_start_at=now, trial_end_at=now+14d
2) At trial end:
- if payment confirmed -> status=active
- else -> status=past_due then status=grace with grace_end_at = now + grace_days (default 7)
3) If unpaid at grace_end_at -> status=suspended
4) Suspension behavior:
- Firm login allowed only for Admin/Super Admin OR show “Account Suspended” page
- Block creating new engagements, exporting, and AI calls
- Option: allow read-only viewing
5) If payment received later -> status=active (resume)

E) BILLING & INVOICING
Implement monthly billing with invoices:
- Every subscription has current_period_start, current_period_end, next_invoice_at
- Daily cron (02:00 PKT) generates invoices due where now >= next_invoice_at and status in (active, past_due, grace)
Invoice calculation:
- Base plan fee (price_snapshot required)
- User overage: max(0, active_users - max_users) * user_overage_pkr
- Office overage: max(0, active_offices - max_offices) * office_overage_pkr
- Engagement packs: if engagements exceed entitlement:
  packs = ceil( (excess_engagements)/engagement_pack_size )
  charge = packs * engagement_pack_pkr
- Tax fields present but can remain 0 for now (subtotal=total)
Generate invoice_no format: INV-YYYY-000001 (sequential).
Set invoice due_at = issued_at + 7 days.
Hourly cron enforces overdue statuses: active -> past_due -> grace -> suspended.

F) USAGE COUNTING (IMPORTANT)
Define usage counters precisely and implement queries:
- active_users_count = firm users where status=Active (exclude suspended/deleted)
- active_offices_count = firm_offices where is_active=true
- engagements_count = engagements created in current “billing year” window OR maintain YTD; pick one rule and apply consistently
Store these in subscription_usage snapshots (period_start, period_end).
Also store AI tokens (in/out) if available; otherwise keep 0.

G) SUPER ADMIN UI (Platform Admin)
Update left menu already exists: Dashboard, Firm Management, Plan Management, Notifications, Audit Logs, AI Configuration.
Enhance Plan Management:
- Show 4 plan cards (Starter/Growth/Professional/Enterprise)
- Each card must display: price, included users, included offices, included engagements, AI type (Platform AI / Custom Key Allowed), storage
- Show “Active subscriptions” count per plan (query)
- “Create Plan” opens modal:
  fields: plan_name, plan_key, base_price_pkr, included_users, included_offices, included_engagements, storage_gb_included,
          platform_ai_included, allows_custom_ai_key,
          user_overage_pkr, office_overage_pkr, engagement_pack_size, engagement_pack_pkr,
          is_public, is_active
- “Edit Plan” allowed only if no active subscriptions OR create new version record (recommended: versioned plans); minimum implement “edit with audit log”.

Firm Management:
- For each firm show: current plan, status, trial end, grace end, next invoice date, outstanding invoices, usage vs limits
- Actions for Super Admin:
  Suspend / Resume
  Extend trial (days) with reason
  Set grace days
  Change plan (immediate or next period)
  Apply discount (optional; can be later)

H) APIs (.NET) + Frontend (Next.js)
Create/Update endpoints:
- GET /api/platform/plans
- POST /api/platform/plans
- PUT /api/platform/plans/{id}
- GET /api/platform/plans/{id}/stats (active subscriptions)
- GET /api/platform/firms (filters: status, plan)
- GET /api/platform/firms/{id}/subscription
- POST /api/platform/firms/{id}/subscription/change-plan
- POST /api/platform/firms/{id}/subscription/suspend
- POST /api/platform/firms/{id}/subscription/resume
- POST /api/platform/firms/{id}/subscription/extend-trial
- GET /api/billing/invoices?firmId=...
- GET /api/billing/invoices/{id}
- POST /api/billing/payments (manual payment posting)
Add RBAC:
- Only Super Admin can access platform routes
- Firm Admin can view own subscription/invoices

I) QUALITY & AUDIT TRAIL
Log billing-critical changes in billing_audit_log:
- plan create/update
- plan change for firm
- suspend/resume
- extend trial/grace
- invoice issue/void
Store before_state/after_state JSON.

J) ACCEPTANCE CHECKLIST (MUST PASS)
- Plan Management shows 4 plans with correct PKR pricing and limits
- Create Plan works and persists in Postgres
- Firm subscription status changes based on trial/grace rules
- Invoice auto-generated monthly with correct lines and totals
- Overages computed correctly from usage snapshots
- Suspended firms cannot create engagement / use AI / export
- Super Admin can suspend/resume and extend trial
- Active subscriptions count per plan accurate
- All key actions written to billing_audit_log

Now implement the above end-to-end:
1) Write/modify Postgres migrations
2) Seed default plans
3) Implement .NET API endpoints + services
4) Implement cron jobs (hosted background service / Hangfire / Quartz) for daily billing and hourly enforcement
5) Update Next.js UI cards, create/edit modal, and firm billing detail screens
6) Test with sample firms (trialing, active, past_due, suspended) and verify invoices.

Output: Provide code changes + migrations + seed script + UI updates + endpoints + cron service.